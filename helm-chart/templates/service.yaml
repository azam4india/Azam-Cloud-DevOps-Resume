apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  labels:
    app: {{ .Release.Name }}
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 31053
  selector:
    app: {{ .Release.Name }}
